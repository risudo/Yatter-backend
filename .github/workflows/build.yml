
name: Build

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: compose-run
        env:
          ENV: ${{seclets.ENV}}
          MYSQL_DATABASE: ${{seclets.MYSQL_DATABASE}}
          MYSQL_USER: ${{seclets.MYSQL_USER}}
          MYSQL_PASSWORD: ${{seclets.MYSQL_PASSWORD}}
          MYSQL_HOST: ${{seclets.MYSQL_HOST}}
        run: docker compose up -d
